.pager
	if request.path.strip('/') == ''
		if paginator.has_pre
			a.round.pre(href=paginator.pre_url, rel='prev') Prev
		if paginator.has_next
			a.round.next(href=paginator.next_url, rel='next') Next
	else
		+h.paginator()